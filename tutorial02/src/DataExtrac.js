

const dataExtraction = (jsonData) => {
   //   const keys = jsonData.map(
   //      (item) => (Object.keys(item))
   //   )
   //   const values = jsonData.map(
   //      (item) => (Object.values(item))
   //   )
}

export default dataExtraction;